export type TransactionType = {
  date: string;
  donor: string;
  email: string;
  event: string;
  amount: number;
  image?: string;
  description?: string;
  order_date?: string;
  end_date?: string;
  timestamp?: string;
};
