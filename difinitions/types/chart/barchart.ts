export type BarchartType = {
  name: string;
  dataset1: number;
  dataset2: number;
};

export type AverageType = {
  revenue_growth: ReactNode;
  total_revenue: ReactNode;
  amount: number;
  date: string;
};
